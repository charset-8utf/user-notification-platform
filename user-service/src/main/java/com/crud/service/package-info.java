/**
 * Слой сервиса (бизнес-логика).
 * <p>
 * Содержит валидацию входных данных, бизнес-правила (email, возраст),
 * преобразование DTO в сущности через маппер и вызов репозитория.
 * Реализует паттерн «Стратегия» для валидации (опционально).
 * </p>
 */
package com.crud.service;