$kind: http-request
name: Обновить роль (идемпотентно для seed USER)
url: "{{baseUrl}}/api/roles/{{roleIdUser}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "USER"
    }
order: 4000
