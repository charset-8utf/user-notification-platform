spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            api-gateway:
              - uri: http://api-gateway:8080

app:
  bff:
    user-service-base-url: http://api-gateway:8080
    notification-service-base-url: http://api-gateway:8080
    insecure-ssl: false
    load-balanced: true
