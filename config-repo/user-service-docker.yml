spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/userdb
  data:
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: kafka:29092

app:
  notification:
    rest:
      base-url: https://notification-service
